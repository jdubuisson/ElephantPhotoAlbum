<!-- Fixed navbar -->
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('elephant_website_homepage') }}">Elephant Photo Album</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('elephant_website_homepage') }}">Home</a></li>
                    <li><a href="{{ path('album') }}">{{ 'elephant.album.mine' | trans }}</a></li>
                    <li><a href="{{ path('shared_album') }}">{{ 'elephant.album.shared' | trans }}</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('elephant_website_about') }}">About</a></li>
     {#
     <p class="navbar-text">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}</p>
     #}
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                </ul>
            {% endif %}
        </div>
        <!--/.nav-collapse -->
    </div>
</nav>